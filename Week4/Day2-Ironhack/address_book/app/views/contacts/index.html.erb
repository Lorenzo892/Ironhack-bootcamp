<ul>
  <% @contacts.each do |contact|%>
    <li> <%= link_to "#{contact.name} - #{contact.email}", "/contacts/personal/:#{contact.id}" %></li>
  <% end %>
  <%= link_to 'Add contacts', '/contacts/new'%>
</ul>
